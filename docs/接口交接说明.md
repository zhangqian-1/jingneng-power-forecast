# 七站总功率预测平台接口说明

当前模型为 `single_step_7station_2025_v1`。本版沿用平台接口，使用重新训练的3天输入、1点输出模型。输出为下一个15分钟时刻的七站合计功率，单位MW，不是7站或19个测点分别预测。

**时间规则：用户已确认历史训练数据使用北京时间（`Asia/Shanghai`）。接口收发UTC，模型内部按北京时间构造历史、未来时间特征及每周状态特征；输出时间格式与本次请求一致。目标服务器联调仍待完成。**

## 1. 请求方式

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/v1/fluxcast/compute` | 接收历史测点，缓存就绪后预测下一个15分钟时刻的1点 |
| GET | `/api/v1/fluxcast/compute/latest` | 查询本接口最近一次成功结果，不触发预测 |

端口默认8000，地址由部署方提供。请求头为 `Content-Type: application/json`，Body为UTF-8 JSON。Compose默认仅允许本机访问，其他机器通过配置好的网关或内网地址调用。服务不内置鉴权和HTTPS，由部署平台负责。

```bash
curl -X POST "http://127.0.0.1:8000/api/v1/fluxcast/compute" -H "Content-Type: application/json" --data-binary @examples/platform_input_example.json
```

Windows PowerShell使用 `curl.exe`。`@文件路径` 表示调用方读取文件并发送内容，不是把路径交给服务。平台也可以直接组装JSON，无需创建文件。

## 2. 输入字段

| 字段 | 类型 | 要求 |
|---|---|---|
| `point_table` | string数组 | 七站全部35个原始测点编码，无重复、无未知编码；顺序不限 |
| `frames` | 对象数组 | 必须96条，表示最近一天历史实测值，不是未来预测值 |
| `frames[].timestamp` | string | UTC日期和时间，96帧格式一致；连续、不重复、对齐15分钟；支持格式见下文 |
| `frames[][测点编码]` | number或null | 测点直接作为frame的key；功率MW、温度摄氏度、湿度% |

每个时刻提交完整七站快照：19个功率、8个温度、8个湿度。编码见 [测点需求清单](测点需求清单.md)，保留 `.` 和 `:`，不替换为下划线。不嵌套 `stations`、`load_points`、`weather_points`。

缺测推荐显式写 `null`（不加引号），也兼容省略该帧的测点字段；两者均按缺测处理。`point_table`仍须列全35个编码，`timestamp`不能省略。正常测量值不带单位或百分号；布尔、非数字及非有限值按缺测处理，JSON不要写非标准的NaN或Infinity。

本服务按全七站接收，无需 `source_id`。`renewable_data` 是调度算法的数据，本预测接口不要求、不读取它，不将它作为模型特征。不会代为调用新能源API。

通常每15分钟提交一次最近96点；初始化时可按日期从早到晚补传。数据在请求内可按时间排序后校验，不允许重复时间或缺少整条记录。无未来天气输入要求。

### 时间约定

平台收发UTC，不要求增加时区字段。输入使用完整的 `YYYY-MM-DD HH:mm:ss` 或 `YYYY-MM-DDTHH:mm:ss`，可不带后缀，或使用 `Z`、`+00:00`、`+0000`；无后缀也按UTC理解。支持1至9位小数秒，但对齐15分钟整刻时小数必须全为0。显式非零偏移（如 `+08:00`）返回400，调用方应先转换成UTC。

输出保留请求的日期时间分隔符、小数秒位数及UTC后缀，不统一改写为空格格式。同一请求的96帧必须使用相同格式，混用返回400且不写入缓存；不同请求可以切换格式。

| 请求末点（UTC） | 唯一预测目标（UTC） |
|---|---|
| `2025-10-19 15:45:00` | `2025-10-19 16:00:00` |
| `2025-10-19T15:45:00Z` | `2025-10-19T16:00:00Z` |
| `2025-10-19T15:45:00.000+00:00` | `2025-10-19T16:00:00.000+00:00` |

服务在缓存及构造模型特征前将UTC转换成北京时间，完整转换年月日、小时、星期；预测目标时间再转回UTC。数值及其对应时刻不变，也不修改原CSV中的观测时刻或数值。该行为不依赖服务器操作系统时区。

例如输入末点为 `2026-09-21 02:00:00` UTC，模型内部对应 `2026-09-21 10:00:00` 北京时间；第一条预测返回 `2026-09-21 02:15:00` UTC，共1点。平台已发UTC时无需额外平移。正式交付时向对接方说明这一内部转换口径，避免把原反馈“算法不进行时区转换”误解为禁止模型内部特征转换。

## 3. 缓存和缺测

| 情况 | 处理 |
|---|---|
| 功率缺测 | 按0；实测0和有限负值保留，不沿用历史功率，不插值 |
| 温湿度缺测且存在该点过去真实值 | 因果前向填充，不用未来值倒填，不假定天气为0 |
| 连续可用历史不足288点 | 本次数据仍缓存，HTTP 200和空结果，原因 `history_not_ready` |
| 天气尚不能构造完整连续历史 | 本次数据仍缓存，HTTP 200和空结果，原因 `weather_history_not_ready` |

重复时间的功率以最新快照为准，最新缺测则补0；天气保留该时刻最新非空观测，再向后填充。重复请求不增加历史点数。时间中断会重新累计连续历史，不自动造时间记录。请求末点早于已有缓存末点，或请求早于保留范围，返回400。

本版默认使用 `/app/runtime/history_single_step_7station_2025_v1_utc_to_asia_shanghai_v1.csv`，缓存时间为模型北京时间；最近成功结果保存到 `/app/runtime/latest_single_step_forecast_utc_to_asia_shanghai_v1.json`，响应时间为UTC。最多保留768点及历史上下文。缓存和最近结果均校验时间规则版本 `utc_to_asia_shanghai_v1`；旧缓存改名仍会拒绝，旧最近结果不会返回。升级后用UTC重新补传历史，至少288个连续可用点才能预测。容器重建须保留本版runtime；不允许多个服务实例同时写同一缓存文件。

可通过 `--history-cache`、`--latest-json` 指定路径，两者不能重合。仅保留这一套缓存与结果，旧版本缓存不会自动导入。

模型实际输入固定为最近288点，不要求收集连续7天数据。768点是缓存保留上限，用于重叠请求合并和天气、状态上下文衔接；其中更早记录不会作为额外时间步送入模型。天气缺测时仍沿用过去真实观测，所需天气初始化长度取决于实际缺测情况。

## 4. 响应字段

成功返回HTTP 200：

| 字段 | 含义 |
|---|---|
| `result_point` | 1条预测，目标时间为输入末点后15分钟 |
| `result_point[].varname` | 固定 `totalPowerForecast`，表示七站总功率预测 |
| `result_point[].timestamp` | 输入最大时间后15分钟开始的未来1点，UTC，格式与本次请求一致 |
| `result_point[].value` | 有限浮点数，MW，保留四位小数 |
| `event_key` | 沿用平台文档的 `JNH.Fluxcast.Compute`；平台接收端须注册相同名称 |

`(varname, timestamp)`组合唯一。输出不包含调度费用、碳排放或实时准确率。当前预测精度须等对应实测到达后评估，不输出固定的历史参考 `accuracy`。

暂时无法预测时仍为HTTP 200，`result_point: []`，并附：

| 字段 | 含义 |
|---|---|
| `reason` | `history_not_ready` 或 `weather_history_not_ready` |
| `message` | 给人阅读的原因说明，平台程序应判断reason而非匹配中文文本 |
| `continuous_points` | 当前连续可用点数；天气尚不可用时可小于实际已缓存点数 |
| `required_points` | 固定288 |
| `missing_weather_points` | 导致当前可用历史缩短的天气测点编码；无则为空数组 |

**HTTP 200不等于预测成功，必须检查 `result_point`。** 不返回 `value: null` 的伪预测记录，无法预测时直接返回空数组。缺测填充、空结果和内部故障写服务日志。

GET `latest`只返回最近一次成功预测，保留该次请求的时间格式，服务重启后也不改写；后续空结果不会覆盖它。首次无结果返回404；调用方必须核对预测目标时间，不能把较早的成功结果当作本次新预测。

## 5. 错误响应

| HTTP | 原因 | 处理 |
|---|---|---|
| 400 | `invalid_request` | JSON、清单、时间或批次错误；本次不入缓存，修正后重发 |
| 404 | `no_forecast`（新接口latest） | 尚无本模型的平台预测；继续补真实历史 |
| 413 | `request_too_large` | 请求超过50 MiB |
| 500 | `prediction_failed` / `latest_result_unavailable` | 计算、存储或读取故障；查看日志，不当成正常预热 |

新接口错误也包含 `event_key`、空 `result_point`、`reason`、`message`。新接口不以409或422表达缺测/预热。计算失败前有效数据可能已缓存，修复后可重发同一批次。未知路由返回404。

## 6. 完整样例与验证

| 样例 | 用途 |
|---|---|
| [平台成功输入](../examples/platform_input_example.json) | UTC 2025-10-18 16:00至10-19 15:45，真实96帧、35个测点 |
| [平台成功输出](../examples/platform_output_example.json) | 真实模型预测：UTC 2025-10-19 16:00的1点 |
| [平台冷启动输入](../examples/platform_input_not_ready.json) | UTC 2025-10-05 16:00至10-06 15:45，空缓存首次提交 |
| [平台冷启动输出](../examples/platform_output_not_ready.json) | 对应输入的200和空结果 |
| [天气缺失输入](../examples/platform_input_weather_not_ready.json) | 在上述冷启动数据上，特意将一个天气点置null的异常测试 |
| [天气缺失输出](../examples/platform_output_weather_not_ready.json) | 真实接口返回的200和天气原因 |

样例将原CSV北京时间换算为UTC，数值保持原观测；成功输出来自真实模型HTTP回放。成功样例需先回放北京时间2025-10-06至10-19（脚本自动转UTC），单独提交一天不能复现成功。天气异常样例不是完整原始观测，不用于准确率评分。

上述样例日期范围用于初始化原始数据中存在长时间缺测的天气点，不是模型输入长度。天气可用时，按时间顺序补传3批、每批96点即可达到288点门槛。

完整本地复测（需要运行依赖和matplotlib，输出目录必须新建）：

```bash
python tests/run_platform_verification.py --output-dir tests/results/platform_adapter/check_01
```

脚本运行真实模型，验证时间格式保留及切换、混合格式拒绝、ISO8601最近结果重启恢复、旧路由已移除及异常响应，再回放73天/7008点并与实测数据计算误差。结果见该目录的 `verification.json` 和 `rolling_accuracy/summary.json`。不连接生产服务，不上传数据，不重新训练。

## 7. 版本使用

本版只保留上述平台接口和六个JSON样例，旧接口不再提供服务。生产包内只有 `single_step_7station_2025_v1` 单点权重，旧96点权重已移出。此前提交 `11f034b` 对应的已发布镜像仍是96点版本；本版须重新构建并验收。
