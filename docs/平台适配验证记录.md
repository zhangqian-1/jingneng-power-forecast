# 平台接口适配验证记录

日期：2026-09-21；基线提交：`a3ff548`。本记录对应仅保留平台接口的源码版本，记录本地验证结果。GitHub容器构建结果以对应提交的Actions记录为准，本记录不表示公司服务器已部署。

## 1. 对接问题落实

| 问题 | 本地实现与状态 |
|---|---|
| 输入结构 | 新增 `POST /api/v1/fluxcast/compute`；35个原始测点、96帧、清单和连续时间校验 |
| 问题3：响应 | `result_point`含96条 `totalPowerForecast`，时间格式统一，value为有限浮点数；事件名沿用 `JNH.Fluxcast.Compute` |
| 问题4：时区 | **待确认**。CSV无时区标记，当前保留原钟面时间；平台收发约定UTC，未作8小时转换，不视为UTC生产对齐已通过 |
| 问题6：历史不足 | HTTP 200、空结果、`history_not_ready`及连续可用点数；继续缓存 |
| 问题7：温湿度 | 保留35测点及因果天气填充；无法构造历史时200空结果、`weather_history_not_ready`及相关测点；记录日志 |
| 缺测表达 | 样例使用null，也兼容省略帧内测点；清单仍列全35点 |

功率缺测按0，真实0和有限负值保留；天气不得从未来回填。模型权重、训练数据及预测融合参数未修改。旧接口兼容代码及四个旧JSON样例已移除；当前源码只保留平台接口、一套缓存及六个配套样例。旧版本保留在Git历史中，不复制到当前交接目录。

## 2. 本地真实模型测试

另已通过26项接口/交付单元检查、8项原预处理检查、活动模型加载及全部权重哈希核验。Compose配置和GitHub工作流YAML可解析；健康检查已验证成功、冷启动、误用旧镜像和内部错误四种分支。以上不替代真实容器构建测试。

在CPU上启动真实HTTP服务测试，使用七站真实CSV，不连接公司平台：

- 冷启动前6天：新接口均返回200空结果，第7天得到96点预测；旧POST/GET路由均返回404。
- 格式错误及过旧请求返回400，历史缓存未改变。
- 提交有时间断档的新数据时重新报告历史不足，不覆盖最近成功结果。
- 故意将高安屯温度测点置null：返回天气历史不可用，原因中列出该测点。
- 服务停止后重新启动：平台接口最近结果和历史恢复，重复预测结果相同。
- 完整滚动回放：2025-10-20至2025-12-31，共73天、7008个评分点；清理后的预测值与清理前逐点完全一致。

| 指标 | 本次结果 |
|---|---|
| MAPE | 10.8778242064% |
| MAE | 261.7887424680 MW |
| RMSE | 357.0757133589 MW |
| R² | 0.7366803356 |

原始记录位于本机 `tests/results/platform_adapter/single_contract_20260921/verification.json`，明细、每日评分和样例在同目录下。生成目录被Git忽略，不随源码提交。可执行README中的完整验证命令重现。

这些结果证明协议适配没有改变原CSV时间标签下的预测值。它们不证明历史CSV属于UTC，也不保证未来真实平台的预测精度。

## 3. 交付边界

GitHub工作流已统一为平台接口的启动、重建恢复、完整测试集评分及镜像导入后预测检查。更新工作流不代表云端已通过；镜像构建及测试状态须按具体提交查看Actions。当前本地未进行Docker容器测试，也未进行公司服务器部署或平台联调。

下一次交付须构建新镜像并记录对应源码提交；原 `042dcd1` 镜像不含本次修改。正式上线前还需数据提供方确认训练时间口径、平台确认接收的事件/变量名及原因字段，并完成真实UTC时间对齐和服务器验收。
